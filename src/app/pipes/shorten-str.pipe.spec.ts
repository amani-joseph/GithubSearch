import { ShortenStrPipe } from './shorten-str.pipe';

describe('ShortenStrPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenStrPipe();
    expect(pipe).toBeTruthy();
  });
});
